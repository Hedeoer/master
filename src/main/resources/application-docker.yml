# docker环境配置
zeta:
  redis:
    database: 0
    ip: hedeoer.ggff.net
    port: 6379
    password: 'hedeoer123'
    ssl:
      enabled: true
  # 数据源配置
  database:
    url: jdbc:mysql://192.168.2.4:3306/zeta_java?serverTimezone=Asia/Shanghai&useSSL=false&characterEncoding=utf8&allowPublicKeyRetrieval=true&useUnicode=true
    username: hedeoer
    password: hedeoer123321
    druid:
      # druid web监控台是否启动
      WebConsoleEnabled: false
      login-username: admin
      login-password: admin

# 日志配置
logging:
  file:
    path: /root/product/data/zeta-java
  config: classpath:logback-spring.xml
